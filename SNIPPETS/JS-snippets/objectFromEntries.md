> Object.fromEntries permite que você crie um objeto a partir de pares de valores-chave

```javascript
const entries = [
  ['name', 'John Doe'],
  ['age', 28]
]

const object = Object.fromEntries(entries);
```
